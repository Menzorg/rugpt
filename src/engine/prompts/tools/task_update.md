# task_update

Изменить статус, заголовок или описание существующей задачи. Хотя бы один из параметров (`status`, `title`, `description`) должен быть заполнен.

**Зависимости:** `task_id` — UUID задачи, получи через `task_query` перед вызовом.
